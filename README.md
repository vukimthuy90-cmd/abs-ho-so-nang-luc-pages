# abs-ho-so-nang-luc-pages

Bản đã mã hoá của Hồ sơ năng lực Agent Boss, chỉ để GitHub Pages phục vụ.
Nội dung được StatiCrypt mã hoá — cần mật khẩu mới xem được.

Nguồn để sửa nằm ở repo riêng: `abs-ho-so-nang-luc` (private).
Không sửa file trong repo này bằng tay — chạy `deploy.sh` bên repo nguồn.
